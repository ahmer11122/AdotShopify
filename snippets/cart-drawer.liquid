{% comment %}
  Cart Drawer Component
  - Slide-out luxury drawer from right edge
  - Brand logo and minimalist title in header
  - Real-time quantity adjustment & deletion via Shopify AJAX API
  - Fluid spring entrance/exit motion
{% endcomment %}

<cart-drawer class="cart-drawer" id="CartDrawer" role="dialog" aria-modal="true" aria-label="Shopping Bag" data-currency="{{ cart.currency.symbol | default: '$' }}">
  <div class="cart-drawer__backdrop" data-cart-close></div>

  <div class="cart-drawer__panel">
    <!-- Header: Brand Logo, Title, Counter & Close Button -->
    <div class="cart-drawer__header">
      <div class="cart-drawer__header-left">
        <a href="{{ routes.root_url }}" class="cart-drawer__logo-link" aria-label="{{ shop.name }}">
          <img
            src="{{ 'logo.png' | asset_url }}"
            alt="{{ shop.name }}"
            class="cart-drawer__logo-img"
            width="36"
            height="39"
            loading="lazy"
          >
        </a>
        <div class="cart-drawer__title-wrap">
          <span class="cart-drawer__title text-micro">SHOPPING BAG</span>
          <span class="cart-drawer__counter text-micro" data-drawer-count>[{{ cart.item_count }}]</span>
        </div>
      </div>

      <button type="button" class="cart-drawer__close" data-cart-close aria-label="Close cart">
        <svg class="cart-drawer__close-icon" width="18" height="18" viewBox="0 0 18 18" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" aria-hidden="true">
          <line x1="1" y1="1" x2="17" y2="17"></line>
          <line x1="17" y1="1" x2="1" y2="17"></line>
        </svg>
      </button>
    </div>

    <!-- Scrollable Body: Cart Items or Empty State -->
    <div class="cart-drawer__body">
      <!-- Items Container -->
      <div class="cart-drawer__items" data-cart-items>
        {% for item in cart.items %}
          {% assign item_handle = item.product.handle %}
          {% assign hoodie_img = 'category-hoodies.jpg' | asset_url %}
          {% assign shirt_img = 'category-shirts.jpg' | asset_url %}
          {% assign trouser_img = 'category-trousers.jpg' | asset_url %}

          {% assign fallback_title = item.product.title %}
          {% assign fallback_img = item.image | image_url: width: 240 %}
          {% assign fallback_price = item.final_line_price | money %}

          {% case item_handle %}
            {% when 'the-complete-snowboard' %}
              {% assign fallback_title = 'Heavyweight Boxy Hoodie' %}
              {% assign fallback_img = hoodie_img %}
              {% assign fallback_price = 'Rs. 4,499' %}
            {% when 'the-collection-snowboard-hydrogen' %}
              {% assign fallback_title = 'Relaxed Poplin Shirt' %}
              {% assign fallback_img = shirt_img %}
              {% assign fallback_price = 'Rs. 3,699' %}
            {% when 'the-collection-snowboard-liquid' %}
              {% assign fallback_title = 'Tailored Pleated Trousers' %}
              {% assign fallback_img = trouser_img %}
              {% assign fallback_price = 'Rs. 4,299' %}
            {% when 'the-collection-snowboard-oxygen' %}
              {% assign fallback_title = 'Heavyweight Zip Hoodie' %}
              {% assign fallback_img = hoodie_img %}
              {% assign fallback_price = 'Rs. 4,899' %}
            {% when 'the-compare-at-price-snowboard' %}
              {% assign fallback_title = 'Oversized Oxford Button-Down' %}
              {% assign fallback_img = shirt_img %}
              {% assign fallback_price = 'Rs. 3,899' %}
            {% when 'selling-plans-ski-wax' %}
              {% assign fallback_title = 'Wide-Leg Minimalist Chino' %}
              {% assign fallback_img = trouser_img %}
              {% assign fallback_price = 'Rs. 3,999' %}
            {% when 'the-inventory-not-tracked-snowboard' %}
              {% assign fallback_title = 'Heavyweight Loopback Crewneck' %}
              {% assign fallback_img = hoodie_img %}
              {% assign fallback_price = 'Rs. 3,999' %}
            {% when 'the-3p-fulfilled-snowboard' %}
              {% assign fallback_title = 'Double-Pleat Cotton Chino' %}
              {% assign fallback_img = trouser_img %}
              {% assign fallback_price = 'Rs. 4,199' %}
            {% when 'the-multi-location-snowboard' %}
              {% assign fallback_title = 'Oversized Fleece Pullover' %}
              {% assign fallback_img = hoodie_img %}
              {% assign fallback_price = 'Rs. 4,699' %}
            {% when 'the-multi-managed-snowboard' %}
              {% assign fallback_title = 'Structured Twill Overshirt' %}
              {% assign fallback_img = shirt_img %}
              {% assign fallback_price = 'Rs. 4,199' %}
            {% when 'the-videographer-snowboard' %}
              {% assign fallback_title = 'Relaxed Straight Trousers' %}
              {% assign fallback_img = trouser_img %}
              {% assign fallback_price = 'Rs. 4,499' %}
            {% when 'the-out-of-stock-snowboard' %}
              {% assign fallback_title = 'Relaxed Camp Collar Shirt' %}
              {% assign fallback_img = shirt_img %}
              {% assign fallback_price = 'Rs. 3,799' %}
          {% endcase %}

          {% assign item_title = item.properties._adot_title | default: fallback_title %}
          {% assign item_image = item.properties._adot_image | default: fallback_img %}
          {% assign item_price = item.properties._adot_price | default: fallback_price %}
          {% assign item_size = item.properties.Size %}

          <div class="cart-drawer__item" data-item-key="{{ item.key }}">
            <a href="{{ item.url }}" class="cart-drawer__item-media" tabindex="-1">
              {% if item_image != blank %}
                <img
                  src="{{ item_image }}"
                  alt="{{ item_title | escape }}"
                  class="cart-drawer__item-img"
                  width="80"
                  height="106"
                  loading="lazy"
                >
              {% elsif item.image %}
                <img
                  src="{{ item.image | image_url: width: 240 }}"
                  alt="{{ item_title | escape }}"
                  class="cart-drawer__item-img"
                  width="80"
                  height="106"
                  loading="lazy"
                >
              {% endif %}
            </a>

            <div class="cart-drawer__item-info">
              {% if item_size != blank %}
                <p class="cart-drawer__item-variant text-micro">Size: {{ item_size }}</p>
              {% elsif item.product.has_only_default_variant == false %}
                {% unless item_handle contains 'snowboard' or item_handle contains 'ski' %}
                  <p class="cart-drawer__item-variant text-micro">{{ item.variant.title }}</p>
                {% endunless %}
              {% endif %}
              <div class="cart-drawer__item-price text-micro">{{ item_price }}</div>

              <div class="cart-drawer__item-actions">
                <div class="cart-drawer__qty">
                  <button type="button" class="cart-drawer__qty-btn" data-qty-change="-1" data-key="{{ item.key }}" aria-label="Decrease quantity">−</button>
                  <span class="cart-drawer__qty-num text-micro">{{ item.quantity }}</span>
                  <button type="button" class="cart-drawer__qty-btn" data-qty-change="1" data-key="{{ item.key }}" aria-label="Increase quantity">+</button>
                </div>
                <button type="button" class="cart-drawer__remove text-micro" data-remove-item data-key="{{ item.key }}">Remove</button>
              </div>
            </div>
          </div>
        {% endfor %}
      </div>

      <!-- Empty Zero-State -->
      <div class="cart-drawer__empty" data-cart-empty {% if cart.item_count > 0 %}hidden{% endif %}>
        <div class="cart-drawer__empty-icon" aria-hidden="true">
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M6 8h12l1.5 13H4.5L6 8z"></path>
            <path d="M9 8V6a3 3 0 0 1 6 0v2"></path>
          </svg>
        </div>
        <h3 class="cart-drawer__empty-title text-micro">YOUR BAG IS EMPTY</h3>
        <p class="cart-drawer__empty-subtitle text-micro">Explore our latest seasonal collections and luxury essentials.</p>

        <div class="cart-drawer__empty-links">
          <a href="{{ routes.all_products_collection_url }}?category=hoodies" class="cart-drawer__empty-link text-micro" data-cart-close>Hoodies Collection</a>
          <a href="{{ routes.all_products_collection_url }}?category=shirts" class="cart-drawer__empty-link text-micro" data-cart-close>Shirts Collection</a>
          <a href="{{ routes.all_products_collection_url }}?category=trousers" class="cart-drawer__empty-link text-micro" data-cart-close>Trousers Collection</a>
          <a href="{{ routes.all_products_collection_url }}" class="cart-drawer__empty-link cart-drawer__empty-link--primary text-micro" data-cart-close>All Essentials →</a>
        </div>
      </div>
    </div>

    <!-- Pinned Footer: Subtotal & Checkout -->
    <div class="cart-drawer__footer" {% if cart.item_count == 0 %}hidden{% endif %}>
      <div class="cart-drawer__subtotal-row">
        <span class="cart-drawer__subtotal-label text-micro">SUBTOTAL</span>
        <span class="cart-drawer__subtotal-val" data-cart-subtotal>{{ cart.total_price | money }}</span>
      </div>
      <p class="cart-drawer__taxes text-micro">Taxes and shipping calculated at checkout.</p>

      <form action="{{ routes.cart_url }}" method="post" class="cart-drawer__form">
        <button type="submit" name="checkout" class="cart-drawer__checkout-btn">
          <span>CHECKOUT</span>
          <svg width="14" height="14" viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <line x1="1" y1="7" x2="13" y2="7"></line>
            <polyline points="8 2 13 7 8 12"></polyline>
          </svg>
        </button>
      </form>

      <a href="{{ routes.cart_url }}" class="cart-drawer__view-cart text-micro">View Full Bag</a>
    </div>
  </div>
</cart-drawer>
